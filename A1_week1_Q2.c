#include <stdio.h>

int main()
{
    int M, N;
    scanf("%d", &M);
    scanf("%d", &N);
    printf("%d", M % N == 0);
    /*here '%' is modulas funtion and arithemtic, Here we use ARITHMETIC OPERATORS
    and  RELATIONAL OPERATORS*/
    return 0;
}
